# Pokémon save backups are stored here.
# Format: <username> <YYYY>_<MM>_<DD>_<HH>_<mm>_<ss>.zip
# This folder is created automatically by PKMswitch on the SD card at runtime.
# It is NOT part of the RomFS bundle.
